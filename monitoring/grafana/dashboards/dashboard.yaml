apiVersion: 1

providers:
  - name: 'ML API Dashboards'
    orgId: 1
    folder: 'ML Pipeline'
    folderUid: 'ml-pipeline'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
